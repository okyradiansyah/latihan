#include <iostream> // library standar yang digunakan untuk input dan output
//I.S Program Sederhana II (Mengunakan Variabel)
//F.S Menampilkan sebauh text di dalam program

using namespace std;

int main( ) {
   char str[] = "Hello C++"; 
   //assignment (=) str adalah variabel yang berisi Hello C++
    
   cout << "Value of str is : " << str << endl;
   // Output << " Isi " << str (Memangil variabel str) << endl (baris baru)
    
    return 0; // Mengakhiri Program
}
